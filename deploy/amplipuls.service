[Unit]
Description=amplipuls.su (Astro + Node)
After=network.target

[Service]
Type=simple
User=amplipuls_su_usr
Group=amplipuls_su_usr
WorkingDirectory=/var/www/amplipuls_su_usr/data/www/amplipuls.su
Environment=NODE_ENV=production
Environment=HOST=127.0.0.1
Environment=PORT=3000
# Если Node установлен через nvm — раскомментируйте ExecStart ниже и закомментируйте прямой вызов node.
ExecStart=/var/www/amplipuls_su_usr/data/.nvm/versions/node/v22.23.2/bin/node dist/server/entry.mjs
# Путь к node после: su - amplipuls_su_usr -c 'source ~/.nvm/nvm.sh && which node'
# ExecStart=/bin/bash -lc 'source /var/www/amplipuls_su_usr/data/.nvm/nvm.sh && nvm use 22 && exec node dist/server/entry.mjs'
Restart=on-failure
RestartSec=5

[Install]
WantedBy=multi-user.target
